steps:
    - id: "Rodando Testes Unitários"
      name: "gcr.io/cloud-builders/go"
      args: ["test"]
      env: ["GOPATH=./src/"]

    - id: "Gerando Imagem"
      name: 'gcr.io/cloud-builders/docker'
      args: ['build','-t','gcr.io/$PROJECT_ID/desafioCI','.']

images: 
  - 'gcr.io/$PROJECT_ID/desafioCI:latest'
    
